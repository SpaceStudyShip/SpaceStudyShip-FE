# Todo List UI/UX Improvement Design

**Date**: 2026-02-16
**Branch**: 20260214_#16_TodoListScreen_전체_화면_및_카테고리_폴더_관리_구현
**Status**: Approved

---

## Problem Statement

TodoListScreen과 CategoryTodoScreen에 5가지 UI/UX 문제:

1. **AppBar 배경**: SpaceBackground 별이 AppBar 영역에서 보이지 않음 (MainShell과 중복 SpaceBackground)
2. **섹션 헤더 크기**: '카테고리', '미분류' 헤더가 `label16Medium`으로 가독성 부족
3. **액션 혼동**: AppBar + 아이콘(할일 추가)과 본문 '+ 추가' 텍스트(카테고리 추가)가 시각적으로 유사
4. **카테고리 카드 레이아웃**: 가로 리스트 카드가 단조로움
5. **onLongPress 비직관적**: 미분류 할일의 카테고리 이동이 길게 누르기로만 가능 → 발견 불가

---

## Solution Design

### 1. AppBar/배경 통합

**현재**: TodoListScreen/CategoryTodoScreen이 자체 SpaceBackground를 갖고 있어 MainShell과 이중 렌더링

**변경**:
- `Scaffold.backgroundColor` → `Colors.transparent` (MainShell 배경 통과)
- `extendBodyBehindAppBar: true` 추가 (body가 AppBar 뒤로 확장)
- body에서 `Stack` + `SpaceBackground` 제거 → 콘텐츠만
- ListView padding에 상단 여유 추가: `MediaQuery.of(context).padding.top + kToolbarHeight`

**효과**: SpaceBackground 인스턴스 1개로 통합 → 성능 향상 + AppBar에 별 표시

### 2. 섹션 헤더 크기 확대

**현재**: `label16Medium` + `textTertiary`
**변경**: `subHeading_18` + `Colors.white`

적용 대상: '카테고리', '미분류' 헤더 모두

### 3. 카테고리 2열 정사각형 그리드

**현재**: `CategoryFolderCard` - 가로 전체 너비 리스트 카드

**변경**:
- `GridView.builder` (shrinkWrap + NeverScrollableScrollPhysics) inside ListView
- `crossAxisCount: 2`, `childAspectRatio: 1.0`, 간격: `AppSpacing.s12`
- CategoryFolderCard를 정사각형 레이아웃으로 수정:
  - 세로 중앙 정렬: 이모지(32sp) → 이름(label_16, white) → 완료율(tag_12, textTertiary)
  - 우상단 ⋮ 아이콘 유지 (삭제용)

### 4. 카테고리 추가 → 그리드 내 빈 카드

**현재**: '+ 추가' 텍스트 (Row 우측) + `_AddCategoryButton` (카테고리 없을 때)

**변경**:
- 그리드 마지막 아이템으로 점선 테두리 + 중앙 폴더+ 아이콘 + '추가' 텍스트
- `_AddCategoryButton` 클래스 삭제
- 카테고리 없을 때도 추가 카드 1개만 표시

### 5. 양방향 스와이프

**현재**: 오→왼 스와이프(삭제) + onLongPress(카테고리 이동)

**변경**:
- `DismissDirection.horizontal` (양방향)
- **왼→오 (startToEnd)**: 카테고리 이동
  - `AppColors.primary` 배경 + `drive_file_move_outline` 아이콘
  - `confirmDismiss` → `showCategoryMoveBottomSheet()` 호출 후 `return false` (아이템 유지)
- **오→왼 (endToStart)**: 삭제 (기존과 동일)
- `onLongPress` 제거

**적용 대상**: TodoListScreen (미분류), CategoryTodoScreen (카테고리 내 할일)

---

## Changed Files

| File | Changes |
|------|---------|
| `todo_list_screen.dart` | 모든 항목 (1-5) 적용 |
| `category_todo_screen.dart` | 항목 1, 5 적용 |
| `category_folder_card.dart` | 항목 3 적용 (정사각형 레이아웃) |

---

## Layout Sketch

```
┌────────────────────────────────────────┐
│ [←]  오늘의 할 일              [+]     │ ← AppBar (transparent, 별 보임)
├────────────────────────────────────────┤
│                                        │
│ 카테고리                    (subH_18)  │
│                                        │
│ ┌──────────┐ ┌──────────┐              │
│ │   📚     │ │   💻     │              │
│ │   수학    │ │  프로그래밍│              │
│ │ 2/5 완료  │ │ 1/3 완료  │              │
│ └──────────┘ └──────────┘              │
│ ┌──────────┐ ┌──────────┐              │
│ │   🎨     │ │  ┌───┐   │              │
│ │   미술    │ │  │ + │   │              │
│ │ 0/2 완료  │ │  └───┘   │              │
│ └──────────┘ │  추가     │              │
│              └──────────┘              │
│                                        │
│ 미분류                      (subH_18)  │
│                                        │
│ ← 📂 카테고리 이동  │  할일 1  │ 삭제 🗑 → │
│ ← 📂 카테고리 이동  │  할일 2  │ 삭제 🗑 → │
│                                        │
└────────────────────────────────────────┘
```
